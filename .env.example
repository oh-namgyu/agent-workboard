# agent-workboard configuration — all optional, no secrets required.
# Server
WORKBOARD_PORT=5054
WORKBOARD_HOST=127.0.0.1
WORKBOARD_DB=~/.agent-workboard/workboard.db
WORKBOARD_TTL_MIN=30
# CLI / hooks
WORKBOARD_URL=http://127.0.0.1:5054
WORKBOARD_AGENT=claude
